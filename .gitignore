node_modules
dist
dist-ssr
.vercel
*.local
tsconfig.tsbuildinfo
.DS_Store

# Unoptimized source photography downloaded from the client site.
# Large, replaceable, and regenerated by `npm run images:fetch`.
raw-images/

# Defensive: internal briefing material with client PII must never enter this repo.
*.pdf
*.docx
*.zip
